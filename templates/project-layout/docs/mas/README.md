# MAS documents

Add your **Micro Architecture Specification (MAS)** documents here.

The agent uses these alongside the HAS to build the requirement model and the
RTL database. Anything missing or ambiguous is flagged `TBD` and raised as a
question rather than guessed.

This folder is registered as `paths.mas_dir` in `rtl_db/index.yaml`.
