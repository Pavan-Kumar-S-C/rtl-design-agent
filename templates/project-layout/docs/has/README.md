# HAS documents

Add your **Hardware Architecture Specification (HAS)** documents here.

The RTL Development & Verification agent reads these to extract requirements
(modules, interfaces, registers, memories, clocks, resets, parameters,
protocols, constraints). It never invents content not present in these docs.

This folder is registered as `paths.has_dir` in `rtl_db/index.yaml`.
