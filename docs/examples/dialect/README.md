# Dialect examples

- Use `../fsm/two_process_fsm.v` for Verilog-2001 FSM style.
- Use `../fsm/two_process_fsm.sv` for SystemVerilog FSM style.

Do not mix `logic` / `always_ff` into `.v` files unless the project already does.
